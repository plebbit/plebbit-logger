{
  "name": "@plebbit/plebbit-logger",
  "version": "0.0.1",
  "keywords": [
    "plebbit",
    "subplebbit"
  ],
  "repository": "github:plebbit/plebbit-logger",
  "license": "GPL-2.0",
  "engines": {
    "node": ">=16.0.0",
    "npm": ">=7.0.0"
  },
  "types": "./dist/src/index.d.ts",
  "files": [
    "src",
    "dist/src",
    "!dist/test"
  ],
  "main": "dist/node/index.js",
  "types": "dist/node/index.d.ts",
  "scripts": {
    "build": "rm -fr dist && tsc",
    "test": "npm run build && DEBUG=plebbit-logger:* node dist/test && DEBUG=plebbit-logger:test:something node dist/test && DEBUG=plebbit-logger:test:* node dist/test && DEBUG=plebbit-logger:error node dist/test && DEBUG=plebbit-logger:trace node dist/test && DEBUG=plebbit-logger:test:something:trace node dist/test"
  },
  "dependencies": {
    "debug": "4.3.3"
  },
  "devDependencies": {
    "@tsconfig/recommended": "1.0.1",
    "@types/assert": "1.5.6",
    "@types/debug": "4.1.7",
    "@types/node": "18.7.7",
    "typescript": "4.5.4"
  }
}
